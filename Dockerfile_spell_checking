FROM python:3-alpine3.18

ENV DIR=/srv/docker/textextraction

# ENV MOUNTFOLDER=/srv/data

# RUN mkdir ${MOUNTFOLDER}/textextraction

# RUN mkdir ${MOUNTFOLDER}/textextraction/watched

# RUN mkdir ${MOUNTFOLDER}/textextraction/watched/spell_checking


RUN mkdir spell_checking

WORKDIR ${DIR}/spell_checking

COPY . ${DIR}/spell_checking

RUN pip install -r requirements.txt

# VOLUME [ "${MOUNTFOLDER}}/textextraction/spell_checking" ]

RUN pip install --editable .

ENTRYPOINT [ "python3" , "src/spell_checking/main.py" ]